# 多城市分层联邦学习 —— 实验结果与问题总结

> 生成日期：2026-08-14
> 数据来源：`outputs/` 下各 run 目录的 `config.json` + `metrics.json`，由 `experiments/organize_results.py` 重映射整理

---

## 一、背景与目标

项目用 CHARGED 6 城市数据集（SZH/AMS/JHB/LOA/MEL/SPO）做多城市分层联邦学习（FL）电动汽车充电负荷预测。核心方法链路为「站点 → 城市 → 全局」两层聚合，城市权重 β_c ∝ N_c^α（α 控制大城主导程度），并叠加 FedBN + LocalHead 处理跨城异质性。

本次任务：停止正在跑的实验后，**盘点现状、整理结果、找出问题、列出需补做的实验**。

---

## 二、整理后的实验结果

> 同实验 3 seeds 求 mean±std。多城市实验用 `macro_city`（每城等权），单城市/基线用 `AVERAGE`（每站平均）。

### 2.1 多城市 FL（6 城市，top_k=10，rounds=50）

| 实验 | n | RMSE | MAE | WAPE |
|---|---|---|---|---|
| fedavg α=0.0 | 3 | 19.93±0.22 | 16.82±0.18 | 34.97±0.29 |
| fedavg α=0.5 | 3 | 19.93±0.22 | 16.82±0.18 | 34.97±0.29 |
| fedavg α=1.0 | 3 | 19.93±0.22 | 16.82±0.18 | 34.97±0.29 |
| fedprox α=0.5 | 3 | 22.07±0.72 | 18.81±0.62 | 40.87±1.74 |
| fedprox α=1.0 | 3 | 22.07±0.72 | 18.81±0.62 | 40.87±1.74 |
| full_method（α=0.5，FedBN+LocalHead） | 3 | 25.67±0.22 | 22.58±0.27 | 53.81±0.85 |
| fedbn_only（α=0.5） | 2 | 31.13±0.36 | 27.08±0.37 | 45.83±0.50 |
| localhead_only（α=0.5） | 2 | 31.13±0.36 | 27.08±0.37 | 45.83±0.50 |
| clustered / clustered_fedbn | — | ❌ OOM 崩溃 | — | — |

### 2.2 单城市基线（SZH，top_k=10，epochs=50）

| 实验 | n | RMSE | MAE | WAPE |
|---|---|---|---|---|
| local_only（孤岛训练） | 3 | 63.00±0.00 | 50.58±0.00 | 39.17±0.00 |
| seasonal_naive（168h） | 3 | 67.57±0.00 | 47.33±0.00 | 35.01±0.00 |
| centralized（集中式） | 3 | 77.37±2.97 | 66.09±3.56 | 82.19±8.63 |

> `local_only`/`seasonal_naive` 的 std=0 是 baseline 脚本忽略 `--seed` 参数（`set_seed(cfg.seed)` 恒用默认 42）造成的，需重跑才有真实方差。

---

## 三、发现的问题（按严重程度排序）

### 🚨 P0 — α（城市平衡）完全没有效果（最严重）

**现象**：fedavg 的 α=0.0/0.5/1.0 三个结果精确到小数点后 15 位完全相同（19.93），fedprox 的 α=0.5/1.0 也完全相同（22.07）。

**根因**：`config.json` 里 α 确实不同，聚合代码 `CityBalancedServer` 也确实用了 α，但 **`top_k=10` 使 6 城都是「10 站点 × 4392 行」→ N_c 全等于 43920**。城市权重 β_c ∝ N_c^α 里所有 N_c 相同 → β_c = 1/6 恒定，α 取任何值都一样。且每个站点 data_size 也全相等（4392），「站点→城市→全局」两层聚合与标准 FedAvg（60 站直接平均）**完全等价**。

**影响**：论文的「城市平衡聚合」和「分层聚合」两个核心贡献在当前设置下是**空操作**。城市间真实的不平衡（见下表）被 `top_k=10` 抹平了。

各城数据清洗后**有效站点数**（`select_top_stations` 三步过滤后）：

| 城市 | 原始站点 | 有效站点 | 占总量比例 |
|------|---------|---------|-----------|
| SZH | 1379 | 752 | 38.8% |
| AMS | 1388 | 986 | 50.9% |
| LOA | 224 | 139 | 7.2% |
| JHB | 35 | 22 | 1.1% |
| MEL | 62 | 22 | 1.1% |
| SPO | 41 | 17 | 0.9% |
| **合计** | | **1938** | 100% |

> 注意：按有效站点数，**AMS（986）比 SZH（752）更大**，不是 SZH 主导。这是数据本身反映出的真实情况，需在论文叙事里核实。

**结论**：多城市实验不是「重跑」而是「重新设计」——必须让城市间数据量真正不平衡（见第四节三方案）。

### ⚠️ P1 — full_method（FedBN+LocalHead）反而更差

- full_method 25.67 vs fedprox 22.07（同为 rounds=50、α=0.5、macro_city）。
- 说明时区归一化后 6 城负荷模式已高度相似，FedBN/LocalHead 这类「异质性处理」机制**没有用武之地，反而减少信息共享**。
- 与 P0 同源：整个「跨城异质性」前提在当前数据上可能不成立。**需和导师讨论论文方向。**

### ⚠️ P1 — centralized（理论上限）垫底

- centralized 77.37 > seasonal_naive 67.57 > local_only 63.00。
- 集中式训练是「理论上限」，却比时序基线还差。可能根因：10 个异质站点 + 全局归一化 + 单模型无法兼顾各站尺度（全局 target scaler 混合大站~200 与小站~5 的量纲，小站信号被压缩）。
- 需进一步排查，或在论文里给出合理解释。

### ⚠️ P2 — 数据缺口

| 缺口 | 现状 | 说明 |
|------|------|------|
| 单城市 FL（fedprox / fedprox_fedbn_lh） | ❌ 丢失 | 被多城市实验覆盖，6 城都要重跑 |
| 多城市消融 fedbn-only / localhead-only | ⚠️ 仅 rounds=10 | 需补 rounds=50 才能与 full_method 公平对比；且两者 rounds=10 结果完全相同（31.13），高度存疑 |
| 留一城市冷启动 | ⚠️ 2/6 | 只跑了 train_AMS、train_SZH |
| clustered / clustered_fedbn | ❌ OOM | exit code -1073740791（GPU 显存溢出） |

---

## 四、城市选站方案（具体实施细节）

> 核心目标：让各城 N_c 真正不同，使 β_c ∝ N_c^α 和两层聚合产生实际效果。三个方案从「改选站逻辑」到「改论文叙事」逐步降级。

### 方案 1（推荐）：按城市规模比例选站

**思路**：保留有限客户端预算（防 OOM），但预算不再「每城等量 top_k」，而是按各城有效站点数占总比例分配，SZH/AMS 多选、小城少选。

**实现位置**：`src/federated/trainer.py` 的 `prepare_multi_city_clients()`，新增 `station_selection="proportional"` 分支；`main.py`/`config.py` 增加该选项。

**具体代码**：

```python
# trainer.py 新增方法
def _allocate_city_budget(self, cities, total_budget, size_metric="count"):
    """
    按各城规模占总比例分配客户端预算 (最大余数法, 每城保底 min_city)。
    size_metric: "count"=有效站点数(等价于 N_c), "volume"=有效站点总充电量
    返回 {city: n_c} 和 {city: size}
    """
    sizes = {}
    for city in cities:
        cd = load_city_data(DATA_DIR, city, self.cfg.data.use_remove_zero)
        # k=极大值 -> 返回全部有效站点 (清洗后), 即城市规模
        all_valid = select_top_stations(
            cd["volume"], self.cfg.data.time_col, k=10**6,
            train_ratio=self.cfg.data.train_ratio + self.cfg.data.val_ratio)
        if size_metric == "volume":
            n_train = max(int(len(cd["volume"]) * (
                self.cfg.data.train_ratio + self.cfg.data.val_ratio)), 1)
            tr = cd["volume"].iloc[:n_train]
            sizes[city] = float(tr[all_valid].sum().sum())   # 总充电量
        else:
            sizes[city] = float(len(all_valid))              # 有效站点数
    total = sum(sizes.values())

    # 最大余数法分配, 保底 min_city (建议 2~3, 避免单站城市模型过噪)
    min_city = getattr(self.cfg.data, "min_city_clients", 2)
    raw = {c: total_budget * sizes[c] / total for c in cities}

    # 1) 先给每城保底 min_city
    alloc = {c: min_city for c in cities}
    remain = {c: raw[c] - min_city for c in cities}   # 保底后剩余额度 (可为负)
    # 2) 剩余额度取整分配 (负额度城保持保底)
    alloc = {c: min_city + max(0, int(remain[c])) for c in cities}
    frac = {c: remain[c] - int(remain[c]) for c in cities}
    # 3) 余数大的城优先补 +1, 直到总配额 == total_budget
    order = sorted(cities, key=lambda c: -frac[c])
    i = 0
    while sum(alloc.values()) < total_budget:
        alloc[order[i % len(order)]] += 1
        i += 1
    return alloc, sizes
```

**在 `prepare_multi_city_clients()` 中接入**：

```python
# 原代码 (每城等量 top_k):
#   stations = select_top_stations(volume, time_col, top_k_stations, ...)
# 改为:
if self.cfg.data.station_selection == "proportional":
    budget, sizes = self._allocate_city_budget(
        cities, self.cfg.data.top_k_stations, size_metric="count")
    self.city_size_log = sizes   # 供打印/校验
    stations = select_top_stations(
        city_data["volume"], self.cfg.data.time_col, budget[city],
        train_ratio=self.cfg.data.train_ratio + self.cfg.data.val_ratio)
else:
    stations = select_top_stations(..., self.cfg.data.top_k_stations, ...)
```

**命令行参数**（`main.py` `parse_args`）：

```python
parser.add_argument("--station_selection", type=str, default="top_k",
    choices=["top_k", "proportional", "stratified_balanced",
             "stratified_natural", "all"])
parser.add_argument("--min_city_clients", type=int, default=2)
```

**预算分配示例**（`total_budget=60`，按有效站点数比例，min_city=2）：

| 城市 | 有效站点 | 比例 | 原始配额 | 最终配额 |
|------|---------|------|---------|---------|
| AMS | 986 | 50.9% | 30.5 | 30 |
| SZH | 752 | 38.8% | 23.3 | 23 |
| LOA | 139 | 7.2% | 4.3 | 4 |
| JHB | 22 | 1.1% | 0.7 | 2（保底） |
| MEL | 22 | 1.1% | 0.7 | 2（保底） |
| SPO | 17 | 0.9% | 0.5 | 2（保底） |
| **合计** | 1938 | | 60 | 63 ⚠️ |

> 上表总配额 63 超出预算 60，因为保底 min_city=2 抬高了 JHB/MEL/SPO。实际操作时需把 `total_budget` 上调到 ≥ 保底和（如 budget=66），或对 min_city=1 的城市允许配额 1。**这是实现时要重点校验的边界**——分配函数必须保证 `sum(alloc) == total_budget` 且每城 ≥ 1。

**α 效果验证**（用分配后的 N_c 计算 β_c，α 三种取值应明显不同）：

| α | β_AMS | β_SZH | β_LOA | β_JHB/MEL/SPO |
|---|-------|-------|-------|---------------|
| 0.0 | 0.167 | 0.167 | 0.167 | 0.167 |
| 0.5 | 0.32 | 0.28 | 0.15 | ~0.05 |
| 1.0 | 0.48 | 0.37 | 0.06 | ~0.02 |

> 验证通过标准：重跑 quick 模式（rounds=10、2 seeds）后，`organize_results.py` 里 fedavg α=0/0.5/1 的 RMSE **不再完全相同**。

**重跑命令**（改完选站逻辑后）：

```bash
# 快速验证 α 是否拉开差距
python experiments/full_experiment.py --mode quick --top_k 60 \
    --station_selection proportional --min_city_clients 2

# 全量重跑多城市核心实验 (rounds=50, 3 seeds)
python experiments/full_experiment.py --mode multi_city \
    --top_k 60 --rounds 50 --station_selection proportional
```

### 方案 2：用全部有效站点

**思路**：`station_selection="all"`，每城选全部有效站点，最忠实于原始分布。客户端总数 = 1938。

**实现**：

```python
# trainer.py 里 select_top_stations 的 k 传极大值即得全部有效站点
stations = select_top_stations(
    city_data["volume"], self.cfg.data.time_col, k=10**6,
    train_ratio=...)
# 或直接复用 _allocate_city_budget 中已取到的 all_valid
```

**必须配套的 OOM/速度控制**（1938 客户端裸跑会 OOM）：

1. **每轮客户端抽样**（标准 FedAvg 做法，新增 `client_fraction` 参数）：
   ```python
   # trainer.py 训练循环里, 每轮只抽 client_fraction 比例的客户端参与
   import random
   k = max(1, int(len(self.clients) * self.cfg.fed.client_fraction))
   active = random.sample(range(len(self.clients)), k)
   ```
   `--client_fraction 0.1` → 每轮约 194 客户端，显存可控。
2. 降低单客户端成本：`--local_epochs 1 --batch_size 64`。
3. 减小模型/输入：`--seq_len 168`（不变），必要时降 `pred_len`。
4. `--top_k` 参数在 all 模式下失效，改由 `client_fraction` 控制实际参与规模。

**代价**：1938 个 client 的 `prepare` 阶段要一次性建好所有 DataLoader 和模型副本，内存占用大（每个 client 一个模型副本）。若仍 OOM，需进一步改造为「按需加载 client 数据」。

**重跑命令**：

```bash
python main.py --cities SZH,AMS,JHB,LOA,MEL,SPO \
    --station_selection all --aggregation fedavg \
    --city_weight_alpha 1.0 --client_fraction 0.1 \
    --num_rounds 50 --local_epochs 1 --top_k 0
```

### 方案 3：维持 top_k=10，改论文叙事

**思路**：代码零改动，现有实验结果（第二节表格）直接可用，但**放弃「城市平衡 α」这条贡献线**，把论文重心移到其他可成立的贡献上。

**具体要做的叙事调整**：

1. 删除「城市平衡聚合 β_c ∝ N_c^α」作为独立贡献——因为当前实验无法支撑（α 无效果）。
2. 主推改成可验证的点，例如：
   - **多城市 FL vs 单城市 FL 的泛化增益**（用 6 城数据联合训练是否优于各自单城训练）——这需要补单城市 FL 基线（第二节 P2 缺口）。
   - **留一城市冷启动**（train 5 城 → 迁移到第 6 城）——这是独立的泛化贡献，不依赖 α。
   - **FedProx / FedBN 的消融**（需补 rounds=50 的 fedbn/localhead 消融）。
3. 若仍想保留「分层聚合」，需先按方案 1 或 2 重做实验，否则审稿人会一眼看出 α 无效（表格里三个 α 数值完全相同就是硬伤）。

**代价**：前两轮已跑的 α 对比实验作废（但 FedAvg/FedProx 的基准值、消融、单城基线仍可用）。

---

## 五、补做 / 重做实验清单

> 顺序：先定选站方案（第四节）→ 再按下面步骤执行。

| 步骤 | 实验 | 前置条件 | 命令（示意） |
|------|------|---------|-------------|
| 1 | 验证选站方案让 α 拉开差距 | 完成方案 1 代码 | `--mode quick --station_selection proportional` |
| 2 | 重跑多城市 α 对比（fedavg/fedprox × α=0/0.5/1） | 步骤 1 通过 | `--mode multi_city --rounds 50` |
| 3 | 补多城市消融 fedbn-only / localhead-only @ rounds=50 | 同选站方案 | `--mode ablation --rounds 50` |
| 4 | 重跑 full_method，确认 FedBN+LocalHead 是否真有害 | 同选站方案 | `--mode multi_city --rounds 50` |
| 5 | 补单城市 FL（fedprox / fedprox_fedbn_lh）@ 6 城 | seed bug 已修 | `--mode single_city --city {各城}` |
| 6 | centralized + local_only @ 3 seeds（有真实方差） | seed bug 已修 | `--mode single_city` |
| 7 | 留一城市冷启动，补剩余 4 城 | — | `leave_one_out.py` |
| 8（可选） | clustered / clustered_fedbn | 降 top_k 或加 client_fraction | — |

---

## 六、关键文件索引

- `experiments/organize_results.py` — 结果整理脚本（按 config.json 重映射，输出 `outputs/summaries/organized_results.json`）
- `src/data/data_loader.py` — `select_top_stations`（三步清洗 + top-k，第 53 行）、`stratified_sample_stations`（分层抽样，第 112 行）
- `src/federated/trainer.py` — `prepare_multi_city_clients`（选站 + 客户端准备，第 188 行）、`city_data_sizes` 计算（第 233 行，top_k=10 下全相等）
- `src/federated/aggregation.py` — `CityBalancedServer`（β_c ∝ N_c^α，第 403 行；逻辑正确，问题在输入 N_c 全相等）
- `main.py` — `--station_selection`/`--city_weight_alpha`/`--cities` 参数与目录命名
- `experiments/full_experiment.py` — 实验矩阵编排（选站方案落地后需同步改这里 + 修 summary 收集路径）
